{
  "name": "oidc-id-jag",
  "version": "0.5.1",
  "summary": "Cross-App Access — Identity Assertion JWT Authorization Grant (ID-JAG)",
  "description": "Implements both ends of draft-ietf-oauth-identity-assertion-authz-grant (Cross-App Access). Issuing side: a confidential client exchanges one of its tokens against a short lived `oauth-id-jag+jwt` assertion targeting another authorization server, answered through the `oidcGotTokenExchange` hook so the token endpoint is left untouched; the requested audience is resolved into the relying party declaring it, whose `TokenXAuthorizedRP` list and access rule are enforced, and the assertion is signed with an asymmetric key so the remote server can verify it against the published JWKS. `subject_token` accepts an ID token, a refresh token or an access token, and RFC 9396 authorization_details travel with the assertion. Consuming side: a client presents an ID-JAG minted by a trusted provider with the RFC 7523 JWT Bearer grant and gets a local access token, after signature, audience, freshness, client and single-use (jti) checks.",
  "author": "Xavier Guimard <yadd@debian.org>",
  "license": "AGPL-3.0",
  "llng_compat": ">=2.23.2",
  "test_depends": [
    "oidc-rar"
  ],
  "perl_requires": {
    "JSON": "2.0",
    "Crypt::JWT": "0"
  },
  "customPlugins": "::Plugins::OIDCIdentityAssertionGrant, ::Plugins::OIDCIdentityAssertionGrantServer",
  "tags": [
    "oidc",
    "oauth2",
    "security"
  ],
  "homepage": "https://github.com/linagora/lemonldap-ng-plugins",
  "autoload": [
    {
      "condition": "or::oidcRPMetaDataOptions/*/oidcRPMetaDataOptionsAllowIdJagGrant",
      "module": "::Plugins::OIDCIdentityAssertionGrant"
    },
    {
      "condition": "or::oidcOPMetaDataOptions/*/oidcOPMetaDataOptionsAllowIdJagGrant",
      "module": "::Plugins::OIDCIdentityAssertionGrantServer"
    }
  ]
}
